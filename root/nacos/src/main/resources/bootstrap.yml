spring:
  application:
    name: nacosdemo #指定了从nacosdemo-[环境标识].yaml 拿取配置 这个name很重要,必须和nacos的配置文件的前缀一样
  profiles:
    active: test #指定使用 bootstrap-dev.yml 还是 bootstrap-test.yml

server:
  port: 8081